document.addEventListener('click', function (e) {
  if (e.target.textContent === '提交审核') {
    // setTimeout(() => {
    //   document.querySelector('.weui-desktop-dialog input[type=checkbox]').click();
    // }, 500);
    // setTimeout(() => {
    //   Array.from(document.querySelectorAll('.weui-desktop-dialog .weui-desktop-btn_primary')).find(item => item.textContent === '下一步').click();
    // }, 500);
    // setTimeout(() => {
    //   Array.from(document.querySelectorAll('.weui-desktop-dialog .weui-desktop-btn_primary')).find(item => item.textContent === '继续提交').click();
    // }, 500);
    // setTimeout(() => {
    //   Array.from(document.querySelectorAll('.weui-desktop-dialog .weui-desktop-btn_primary')).find(item => item.textContent === '下一步').click();
    // }, 500);
  }
})